{
  "rules": {
    ".read": false,
    ".write": false,
    "games": {
      ".read": true,
      ".write": true
    },
    "saves": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
