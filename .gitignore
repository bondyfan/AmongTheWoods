node_modules/
dist/
.DS_Store
*.log
# Streamed biome music tracks — hour-long, up to 127 MB; exceed GitHub's 100 MB limit.
# Kept on disk for local play; host via LFS/CDN for deploy.
assets/music/biome_*.mp3
